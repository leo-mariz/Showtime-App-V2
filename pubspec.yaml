name: app
description: "O Showtime é um marketplace para conectar artistas e anfitriões."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 2.1.0+2

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  build_runner: ^2.10.4
  dart_mappable: ^4.6.1
  auto_route: ^11.0.0
  cloud_firestore: ^6.1.0  # Adicione esta linha
  get_it: ^9.2.0
  firebase_auth: ^6.1.2
  flutter_auto_cache: ^0.0.4
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4
  local_auth_android: ^1.0.48
  local_auth_darwin: ^1.4.3
  flutter_bloc: ^9.1.1
  internet_connection_checker: ^3.0.1
  uni_links: ^0.5.1
  cached_network_image: ^3.4.1
  flutter_custom_tabs: ^2.4.1
  url_launcher: ^6.3.2
  firebase_messaging: ^16.0.4
  flutter_local_notifications: ^19.5.0
  image_picker: ^1.2.1
  google_fonts: ^6.3.3
  firebase_storage: ^13.0.4
  table_calendar: ^3.1.2
  dartz: ^0.10.1
  flutter_dotenv: ^6.0.0
  mailer: ^6.6.0
  equatable: ^2.0.7
  mask_text_input_formatter: ^2.9.0
  cpf_cnpj_validator: ^2.0.0
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  video_player: ^2.10.1
  google_maps_flutter: ^2.9.0
  google_places_flutter: ^2.0.9
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  permission_handler: ^11.3.1
  http: ^1.2.2
  firebase_core: ^4.3.0
  firebase_app_check: ^0.4.1+3
  file_picker: ^10.3.8
  dart_geohash: ^2.1.0
  dart_firebase_admin: ^0.4.1
  cloud_functions: ^6.0.5
  uuid: ^4.5.1
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  dart_mappable_builder: ^4.6.3
  auto_route_generator: ^10.4.0
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - assets/images/profile/profile_default.png
    - assets/images/profile/artist.png
    - assets/icons/logo/Logo.png
    - assets/icons/app_icon/icon.png
    - assets/icons/social_media/instagram.png
    - assets/icons/social_media/spotify.png
    - assets/icons/social_media/youtube.png
    - assets/icons/social_media/tiktok.png
    - .env
    - assets/map_style.json
    - assets/images/preferences/blues.webp
    - assets/images/preferences/classica.jpeg
    - assets/images/preferences/eletronica.jpeg
    - assets/images/preferences/forro.jpeg
    - assets/images/preferences/funk.webp
    - assets/images/preferences/gospel.webp
    - assets/images/preferences/jazz.jpeg
    - assets/images/preferences/mpb.webp
    - assets/images/preferences/pagode.jpeg
    - assets/images/preferences/pop.jpeg
    - assets/images/preferences/rap.png
    - assets/images/preferences/reggae.webp
    - assets/images/preferences/rock.jpeg
    - assets/images/preferences/samba.jpeg
    - assets/images/preferences/sertanejo.jpeg
    - assets/images/preferences/soul.jpeg

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/app_icon/icon.png"
  image_path_ios: "assets/icons/app_icon/ios_icon.jpg"
  adaptive_icon_foreground: "assets/icons/app_icon/icon.png"
  adaptive_icon_background: "#3e3f40"
